/** Nested edit sheet to open when editing a search from Filters / deep link. */
export type SearchEditSection =
  | "location"
  | "platforms"
  | "makes"
  | "price"
  | "year"
  | "mileage"
  | "models"
  | "keywords";
