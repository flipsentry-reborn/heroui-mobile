/**
 * Soft gray blurhash — same as mobile-app `DEFAULT_IMAGE_BLURHASH`.
 * Used as expo-image placeholder while loading or when no URI is available.
 */
export const DEFAULT_IMAGE_BLURHASH = "LHFYV]O]=sbIBZ~UE3t7$#NHoc$%";

export const DEFAULT_IMAGE_PLACEHOLDER = {
  blurhash: DEFAULT_IMAGE_BLURHASH,
} as const;
